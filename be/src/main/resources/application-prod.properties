# Production profile — disable public Swagger UI
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false

# Security defaults
permission.enforcement=true
cookie.secure=true

# Search engine (override via env)
search.engine=${SEARCH_ENGINE:elasticsearch}

# Do not refresh demo catalog images on every deploy
catalog.seed.force=false
review.seed.force=false
